1) Which registers contain arguments to functions? 
For example, which register holds 13 in main's call to printf?

- a0 ~ a7 registers hold the arguments to the function.
- since 13 is the 3rd argument in printf, it was held by a2

2) Where is the call to function f in the assembly code for main? Where is the call to g? 
(Hint: the compiler may inline functions.)
- for f:
26: 45b1  li  a1,12 
- for g:
 14: 250d     addiw a0,a0,3
 1a: 8082     ret

 3) At what address is the function printf located?
0000000000000616 <printf>:

4)
jalr instruction places the address of the next instruction (the “return address”) into the specified register (commonly ra).
 34: 5e6080e7      jalr    1510(ra)  # 616 <printf>
 38: 4501          li      a0,0
The jalr instruction itself is at 0x34.
The next instruction is at 0x38.
5)

He110 Wold
for big endian, unsigned int i = 0x726c6400;
no need to change 57616

printf will correctly consume the 3 for the x=%d part.
Then it will look on the stack (or in registers, depending on the calling convention) for another integer to print for y=%d. But since you did not actually supply a second integer, printf will pull whatever garbage happens to be in that location.

